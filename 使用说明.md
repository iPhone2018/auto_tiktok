# 抖音火花助手 · 商用使用说明

单机版授权模式:**无服务器、无账号、无登录页**。程序绑定机器码,卡密离线验签。
打开程序直接进首页 → 初始化浏览器 → 抖音登录 → 授权信息页展示「抖音标识 + 机器码」→ 卖家发卡激活。
**只有激活且未过期,才能发送消息 / 创建任务**;其余功能(登录、查看、截图、导出 Cookie)不受限制。

---

## 一、卖家侧操作流程

### 1. 一次性初始化(生成密钥对)

```bash
cd 项目目录
.venv/bin/python 卡密生成器.py init
```

- 打印一行 `_LICENSE_PUBLIC_KEY_OBFUSCATED = '...'`(混淆形式,用户看不到明文公钥)→ **粘贴进 `抖音自动续火花-后端.py` 的对应常量**。没有 dev 模式:**未配置公钥程序拒绝启动**(fail-closed),不存在"不校验"的旁路
- 生成 `seller_ed25519_private.pem` 私钥 → **离线保管,绝不进 git、绝不随程序分发**(泄露 = 任何人都能自制卡密)

### 2. 通过 GitHub Actions 生成 exe

1. 把代码推到 GitHub(文件清单见第三节,公钥已粘贴)
2. 仓库 → Actions → **Build Windows EXE** → Run workflow
3. 完成后在 Action 页面底部下载 `DouyinSparkHelper-exe` 产物(保留 30 天)
4. (macOS 版在本地 `./build_mac.sh` 打包)

### 3. 发卡

买家发来 **抖音标识 + 机器码 + 需要的天数**,你执行:

```bash
.venv/bin/python 卡密生成器.py gen -m <机器码> -d 90 -n 1
```

把输出的 `DY-XXXX-...` 发给买家。**建议登记 card_id ↔ 机器码 ↔ 抖音标识 ↔ 买家**。

### 4. 换绑政策

买家**重装系统** → 机器码变化、卡密失效 → 买家发新机器码,你免费重新 `gen` 一张(零成本)。
卡密一次激活后不可重复使用(程序本地记录已用卡号,删数据也拦得住)。

---

## 二、买家侧操作流程

1. 双击打开程序(禁止多开,自动弹出浏览器界面直接进首页)
2. 首页提示「浏览器未初始化」→ 点击初始化(后台无头浏览器,看不到窗口)
3. 设置 → **扫码登录 / 验证码登录** 抖音(二维码显示在界面内)
4. 设置 → 授权信息 → 复制「**抖音标识**」和「**机器码**」发给卖家(未登录抖音时抖音标识为空,视为未激活)
5. 收到卡密 → 授权信息卡 → 续费激活 → 粘贴卡密 → 激活成功(时长从激活时刻起算)
6. 添加定时任务 → 保持程序运行(建议开启「开机自启」)

## 三、上传 GitHub 的文件清单

**要上传:**
```
抖音自动续火花-后端.py        (已粘贴公钥)
卡密生成器.py                 (卖家工具;无私钥内容,公钥可公开)
src/  public/  index.html  vite.config.js
package.json  package-lock.json
requirements.txt
.github/workflows/build.yml   (Actions 构建)
.gitignore                    (已配置排除项)
test_license.py  test_api.py  (测试,可选)
使用说明.md  README.md        (文档)
```

**绝不能上传**(已在 .gitignore 排除):
```
seller_ed25519_private.pem    (私钥!)
chrome_profile/  douyin_spark.db*  .spark_lic  app.lock
.venv/  node_modules/  dist/
```

## 四、交付前 Windows 手工测试清单

在干净的 Win10/11 机器上逐项验证(开发机为 macOS,Windows 分支无法在开发机实测):

- [ ] 双击 exe 启动自动打开浏览器;再开一次被拦截(单实例)
- [ ] 首页直接可见(无登录页);点初始化浏览器成功
- [ ] 扫码/验证码登录全流程;授权信息卡正确显示抖音标识与机器码
- [ ] 未激活时点发送/建任务 → 提示「未激活授权」;激活后正常
- [ ] 机器码重启不变;系统时间回拨 → 时间异常锁定
- [ ] 删除 `%APPDATA%\DouyinSpark\douyin_spark.db` → 重启授权仍在(标记文件恢复)
- [ ] 定时任务重启自动恢复;杀软误报处理;断网机器放 chromedriver.exe 到 exe 同目录可用

## 五、常见问题

| 问题 | 说明 |
|---|---|
| 首次启动慢/提示驱动下载 | chromedriver 需联网下载一次(约 10MB),之后离线可用 |
| 杀软报毒 | Nuitka 单文件打包常见误报,加白名单或购买代码签名证书 |
| 定时任务没执行 | 程序必须保持运行(可开机自启);设置页可看任务日志 |
| 抖音登录掉线 | 每 60 秒自动检测,掉线暂停任务,重新登录后自动恢复补发 |
| 用户换电脑/重装系统 | 机器码变化,走换绑政策(卖家免费重发) |
| 用户调系统时间白嫖 | 程序检测时钟回拨并硬锁 |

## 六、开发模式

```bash
.venv/bin/python 抖音自动续火花-后端.py     # 后端 9844(必须已配置公钥,否则拒绝启动)
npm run dev                                 # 前端 5173(vite 代理 /api)
```

自动化测试可用环境变量 `SPARK_LICENSE_PUBLIC_KEY` 注入测试公钥(优先级高于内置值)。

测试:`test_license.py`(授权单测)、`test_api.py`(接口端到端自测)。
